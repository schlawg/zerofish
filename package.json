{
  "name": "zerofish",
  "version": "0.0.7",
  "author": "T-bone Duplexus",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "main": "dist/zerofish.js",
  "dependencies": {
    "@types/node": "^20.4.2",
    "@types/web": "^0.0.84",
    "@types/emscripten": "^1.39.6",
    "esbuild": "^0.18.4",
    "typescript": "^5.1.6"
  },
  "scripts": {
    "node": "tsc && node dist/test/node",
    "wasm": "wasm/build.sh docker",
    "web": "pnpm wasm && tsc",
    "test": "pnpm web && cp dist/zerofish* ../lila-local/public/compiled",
    "clean": "rm wasm/Makefile -rf dist && if [ -d wasm/lc0 ]; then git -C wasm/lc0 clean -fdx -e *.pb.h; fi && if [ -d wasm/Stockfish ]; then git -C wasm/Stockfish clean -fdx; fi"
  },
  "files": [
    "dist/zerofish.d.ts",
    "dist/zerofish.js",
    "dist/zerofishEngine.wasm",
    "dist/zerofishEngine.js",
    "dist/zerofishEngine.worker.js"
  ]
}
