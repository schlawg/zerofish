{
  "name": "lc0-browser",
  "version": "1.0.0",
  "private": true,
  "author": "Thibault Duplessis",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "dependencies": {
    "@types/node": "^20.4.1",
    "@types/web": "^0.0.84",
    "@types/emscripten": "^1.39.6",
    "typescript": "^5.1.5"
  },
  "scripts": {
    "test": "tsc && cd dist && node ./test/node",
    "wasm": "wasm/build.sh",
    "clean": "rm -rf dist && if [ -d wasm/lc0 ]; then git -C wasm/lc0 clean -fdx; fi && if [ -d Stockfish ]; then git -C Stockfish clean -fdx; fi"
  },
  "keywords": [ ]
}
