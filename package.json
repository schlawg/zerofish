{
  "name": "zerofish",
  "version": "0.0.1",
  "private": true,
  "author": "Lichess",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "dependencies": {
    "@types/node": "^20.4.2",
    "@types/web": "^0.0.84",
    "@types/emscripten": "^1.39.6",
    "esbuild": "^0.18.4",
    "typescript": "^5.1.6"
  },
  "scripts": {
    "node": "wasm/build.sh node && tsc && node dist/test/node",
    "wasm": "wasm/build.sh",
    "web": "pnpm wasm && tsc",
    "test": "pnpm web && cp dist/zerofish* ../lila-local/public/compiled",
    "clean": "rm wasm/Makefile -rf dist && if [ -d wasm/lc0 ]; then git -C wasm/lc0 clean -fdx -e *.pb.h; fi && if [ -d wasm/Stockfish ]; then git -C wasm/Stockfish clean -fdx; fi"
  },
  "keywords": [ ]
}
